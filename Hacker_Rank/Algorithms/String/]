#include <iostream>
#include <string>
using namespace std;

int main(){
	string s;
	cin >> s;
	int s_count;
	s_count = s.length()/3;
	for(int i = 0; i < s.length()-2; i+=2){	
		if(s.substr(i, 3) == "SOS"){
			s_count -= 1;
		}
		if(i > 0 || s.substr())
	}
	cout << s_count << endl;
	return 0;
}
